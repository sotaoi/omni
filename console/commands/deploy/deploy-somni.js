#!/usr/bin/env node

const { buildSomniRoutine } = require('@sotaoi/omni/console/commands/routines/build-somni-routine');

const main = async () => {
  //

  await buildSomniRoutine();

  //
};

main();
